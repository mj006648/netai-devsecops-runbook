# Moved: Kubernetes Incidents

Kubernetes incident records moved to [`../kubernetes/incidents/`](../kubernetes/incidents/).

Use the new path for dated outages, postmortems, and cross-component failure records.

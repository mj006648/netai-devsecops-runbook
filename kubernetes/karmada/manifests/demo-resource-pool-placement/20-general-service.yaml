apiVersion: v1
kind: Service
metadata:
  name: demo-pool-general-nginx
  namespace: demo-resource-pool
spec:
  selector:
    app: demo-pool-general-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

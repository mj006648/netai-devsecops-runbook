apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-multi-edge-nginx
  namespace: demo-multi-rebalance
  labels:
    app: demo-multi-edge-nginx
    scalex.io/workload: edge
spec:
  replicas: 3
  selector:
    matchLabels:
      app: demo-multi-edge-nginx
  template:
    metadata:
      labels:
        app: demo-multi-edge-nginx
        scalex.io/workload: edge
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80

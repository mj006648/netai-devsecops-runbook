apiVersion: v1
kind: Service
metadata:
  name: demo-pool-fallback-rebalance-nginx
  namespace: demo-resource-pool-rebalance
spec:
  selector:
    app: demo-pool-fallback-rebalance-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

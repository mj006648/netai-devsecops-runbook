apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-failover-enabled-nginx
  namespace: demo-failover-enabled
  labels:
    app: demo-failover-enabled-nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: demo-failover-enabled-nginx
  template:
    metadata:
      labels:
        app: demo-failover-enabled-nginx
    spec:
      containers:
        - name: nginx
          image: nginx:1.27
          ports:
            - containerPort: 80

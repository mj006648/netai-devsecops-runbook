apiVersion: v1
kind: Service
metadata:
  name: demo-pull-mode-nginx
  namespace: demo-pull-mode
  labels:
    app: demo-pull-mode-nginx
    scalex.io/demo: pull-mode
spec:
  type: ClusterIP
  selector:
    app: demo-pull-mode-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: demo-taint-tolerated-nginx
  namespace: demo-taint-tolerated
  labels:
    app: demo-taint-tolerated-nginx
spec:
  type: ClusterIP
  selector:
    app: demo-taint-tolerated-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: demo-multi-render-nginx
  namespace: demo-multi-rebalance
spec:
  selector:
    app: demo-multi-render-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

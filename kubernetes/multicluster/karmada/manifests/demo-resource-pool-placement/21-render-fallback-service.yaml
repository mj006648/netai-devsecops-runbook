apiVersion: v1
kind: Service
metadata:
  name: demo-pool-render-fallback-nginx
  namespace: demo-resource-pool
spec:
  selector:
    app: demo-pool-render-fallback-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: demo-scalex-data-nginx
  namespace: demo-scalex-role
spec:
  selector:
    app: demo-scalex-data-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

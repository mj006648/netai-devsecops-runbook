apiVersion: v1
kind: Service
metadata:
  name: demo-scalex-render-nginx
  namespace: demo-scalex-role
spec:
  selector:
    app: demo-scalex-render-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: demo-twinx-auto-nginx
  namespace: demo-twinx-auto
  labels:
    app: demo-twinx-auto-nginx
    scalex.io/target: twinx
spec:
  type: ClusterIP
  selector:
    app: demo-twinx-auto-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

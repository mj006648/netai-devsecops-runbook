apiVersion: v1
kind: Service
metadata:
  name: demo-override-platform-nginx
  namespace: demo-override-platform
spec:
  selector:
    app: demo-override-platform-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

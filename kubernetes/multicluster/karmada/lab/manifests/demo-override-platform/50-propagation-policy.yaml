apiVersion: policy.karmada.io/v1alpha1
kind: PropagationPolicy
metadata:
  name: demo-override-platform-policy
  namespace: demo-override-platform
spec:
  resourceSelectors:
    - apiVersion: apps/v1
      kind: Deployment
      name: demo-override-platform-nginx
    - apiVersion: v1
      kind: PersistentVolumeClaim
      name: demo-override-platform-data
    - apiVersion: v1
      kind: Service
      name: demo-override-platform-nginx
  placement:
    clusterAffinity:
      clusterNames:
        - twinx
        - edgex
        - datax
        - poolx
    replicaScheduling:
      replicaSchedulingType: Divided
      replicaDivisionPreference: Weighted
      weightPreference:
        staticWeightList:
          - targetCluster:
              clusterNames:
                - twinx
            weight: 1
          - targetCluster:
              clusterNames:
                - edgex
            weight: 1
          - targetCluster:
              clusterNames:
                - datax
            weight: 1
          - targetCluster:
              clusterNames:
                - poolx
            weight: 1

apiVersion: v1
kind: Service
metadata:
  name: demo-scalex-edge-nginx
  namespace: demo-scalex-role
spec:
  selector:
    app: demo-scalex-edge-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80

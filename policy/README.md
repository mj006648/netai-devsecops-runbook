# Moved: Kubernetes Security

Policy and webhook runbooks moved to [`../kubernetes/security/`](../kubernetes/security/).

Use the new path for Kyverno, cert-manager, OpenBao, External Secrets, RBAC, and admission/webhook notes.
